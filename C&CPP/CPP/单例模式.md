单例模式就是确保**一个类只有一个实例**，并提供一个**全局访问点**，适合管理那些**天然就应该唯一**的资源或服务。
```CPP
class Singleton
{
public:
	static Singleton& Get() {
		static Singleton instance;
		return instance;
	}
	void test() {
		std::cout << "test" << std::endl;
	}
};
```
第一次调用Get函数的时候会实例化，第二次第三次后就不会再创建新的对象了，确保了只有一个实例。