```CPP
std::cout << "Hello!" << std::endl;
```
在这代码中，发生了`重载操作符`。有人会有疑问，这里的<<不是位移操作符吗，怎么在这里可以输入了。这就不得不提到一个概念：重载。
## 重载
重载是C++中一种编译期多态机制，允许在同一作用域内使用相同的名字（函数名或运算符符号）来表示多个不同的函数实现，编译器根据上下文（主要是参数的个数、类型、顺序，以及对于运算符还包括操作数的类型）自动选择最匹配的那一个版本来调用。简单来讲，就是同一个东西，在不同的Context下可以有不同的功能。