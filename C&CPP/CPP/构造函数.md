## 默认构造函数
默认构造函数是**不需要参数就能调用的构造函数**，包括：
- 无参构造函数
- 所有参数都有默认值的构造函数
## 构造函数
构造函数是类中用于初始化对象的特殊成员函数，可以有多个（重载）。
## 禁止类外实例化（通过更改构造函数）
```CPP
People() = delete;
```
这样会使People()没有默认构造函数。
```CPP
private:
	People() {
		m_x = 0;
		m_y = 0;
	}
```
或者我们让默认构造函数变成一个私有函数，外部无法访问。