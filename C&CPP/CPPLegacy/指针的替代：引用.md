## 引用基本用法
```C++
int main() {
	int a = 3;
	int &c = a;
	c = 0;
	printf("%d", c);
}
```
也就是说，a和b在此时指向了同一块内存空间，他们是完全等价的。
## 用引用进行变量交换
```C++
void swap(int& a, int& b) {
	int temp = b;
	b = a;
	a = temp;
}
int main() {
	int a = 3;
	int b = 9;
	swap(a,b);
	printf("%d %d", a, b);
}
```
