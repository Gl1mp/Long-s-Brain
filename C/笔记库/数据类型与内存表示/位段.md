位段和结构体是很类似的。就是声明成员变量的时候多了一个冒号和后面所占的比特位。
## 位段的存储
他的存储也存在双层对齐，内部的位对齐和外部的对齐属性。
理解起来其实也很直观，不需要考虑过多的对齐。
```C
struct A
{
	int _a:2;
	int _b:5;
	int _c:10;
	int _d:30;
};
```
看到都是整型，开辟4字节。前三个都存进去了，发现最后一个存不进去。再开四个字节。所以大小是八个字节。
## 注意
位段的可移植性是不好的。最好不要各种类型在一个位段中混用。