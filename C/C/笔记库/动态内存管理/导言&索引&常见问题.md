动态内存是存储在内存中的堆区中。而用`int a[100]`此类方式申请的空间都在栈区，无法修改长度。在程序结束时，一般会自动释放空间。但是主动free是一个良好的习惯。如果需要频繁的申请空间，可以尝试维护一个内存池。
## 函数
[[calloc()]]
[[malloc() & free()]]
[[realloc()]]
## 常见问题
1. 对空指针解引用
2. 用`free()`释放空间的一部分
3. 越界访问
4. 用`free()`释放非动态开辟空间
5. 对一块动态开辟空间多次释放
6. 忘记释放：程序提前返回，没有机会执行最后的释放 or 被调函数没有实现释放